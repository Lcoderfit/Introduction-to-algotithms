198. 打家劫舍.go
221. 最大正方形.go
1143. 最长公共子序列.go

```text
1.二维dp，创建二维数组的第二维可以放入dp的双层循环中进行
2.byte切片中元素为'1'，则其对应值为49;
fmt.Scanf("%c", &b[0])输入语句中，b[0]的值是由输入模式%c控制的，例如终端输入：1，格式为%c代表输入的是字符'1',
%d表示输入的是数字1，%v表示输入的是一个字节类型，结果也是数字1
3.二维dp中，对于第一行和第一列的处理可以放入dp的双层循环中：if i == 0 || j == 0
4.如果dp[i]只依赖于dp[i-1]，dp[i-2];那么可以借助两个辅助变量来滚动更新（滚动数组）
5.两个字符串之间的动规，一般都是二维dp，考虑dp[i][j]与dp[i-1][j]和dp[i][j-1]之间的关系
```